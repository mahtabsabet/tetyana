import { defineConfig } from 'vite';

export default defineConfig({
  // Change 'tetyana' to your GitHub repo name when deploying
  // e.g. base: '/tetyana/'
  base: '/tetyana/',
  build: {
    outDir: 'dist',
  },
});
